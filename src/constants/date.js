const STAR_DAYS = Object.freeze([3, 10, 17, 24, 25, 31]);

const WEEKENDS = Object.freeze([1, 2, 8, 9, 15, 16, 22, 23, 29, 30]);

const START_DATE = 1;
const END_DATE = 31;

export { STAR_DAYS, WEEKENDS, START_DATE, END_DATE };
